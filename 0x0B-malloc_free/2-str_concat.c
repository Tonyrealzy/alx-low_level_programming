#include "main.h"
#include <stdlib.h>

/**
 * str_concat - concatenates two strings
 * s1: string
 * s2: string
 * Return: s1, s2 or NULL
 */

char *str_concat(char *s1, char *s2)
{
	char *p;
	int i,j,k,l;

	if (s1 == NULL)
		s1 = "";
	if (s2 == NULL)
		s2 = "";

	while (i = 0; s1[i] != '\0')
		i++;
	while (j = 0; s2[j] != '\0')
		j++;

}
